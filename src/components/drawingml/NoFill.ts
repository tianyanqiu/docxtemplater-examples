import React from 'react';

/**
 * 无填充。
 *
 * 此元素指定没有填充于父元素。
 *
 * 参考文档：https://docs.microsoft.com/zh-cn/dotnet/api/documentformat.openxml.drawing.nofill?view=openxml-2.8.1
 */
const NoFill = () => {
  return React.createElement('a:noFill');
};

export default NoFill;
